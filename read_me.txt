This repo contains Axonect gateway (dataplane)

** at the moment, if a datatype validator added,
if that parameter(header,path or query) is not with the incoming http request:
Gateway will allow it through

** if we do not define a httpvalidator sometimes gateway gets messy

